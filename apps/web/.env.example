# Base URL of the EduFees API. Used by the axios client / TanStack Query hooks.
# Must be an absolute URL (the static-export basePath does not apply to API calls).
NEXT_PUBLIC_API_URL=http://localhost:4000
