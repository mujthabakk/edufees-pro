import { AccountantLayout } from "@/modules/accountant/_layout";
export default function Layout({ children }: { children: React.ReactNode }) {
  return <AccountantLayout>{children}</AccountantLayout>;
}
